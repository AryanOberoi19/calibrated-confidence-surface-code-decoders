# Pinned versions checked on 23 Sep 2026: macOS arm64 wheels exist for Python 3.12.
# Install:  python3.12 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt
stim==1.16.0
pymatching==2.4.0
sinter==1.16.0
beliefmatching==0.2.0
ldpc==2.4.1
numpy==2.2.6          # beliefmatching 0.2.0 requires numpy<=2.2.6
scipy==1.17.1
scikit-learn==1.9.1
matplotlib==3.11.2
torch==2.14.0         # MPS backend on Apple silicon
pytest==9.1.1
